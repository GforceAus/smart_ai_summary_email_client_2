services:
  smart-email:
    build: .
    env_file: .env
    environment:
      UV_CACHE_DIR: /tmp/uv-cache
    volumes:
      - ./data:/app/data
    restart: "no"
    command: uv run python -m src.runners.full_run
